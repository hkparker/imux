package main

import ()

// Load or generate a new self-signed TLS certificate
func serverTLSPair() {
	// Try to return a TLS certificate pair from ~/.imux/server.[crt|key]

	// Create a new self-signed TLS certificate if missing and return it
}
